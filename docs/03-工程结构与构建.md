# 03 工程结构与构建

## 1. 目录布局

```
adoodo/
├── CMakeLists.txt              # 顶层：Qt6 组件 + add_subdirectory(src)
├── README.md
├── docs/                       # 本目录（过程文档）
├── scripts/
│   ├── faster_whisper_bridge.py       # faster-whisper Python 桥（ASR 引擎 B）
│   └── japanese_tokenizer_bridge.py   # janome/fugashi 日语分词桥（M11，可选增强）
└── src/
    ├── CMakeLists.txt          # 全部源文件清单
    ├── main.cpp
    ├── app/        Settings / AppController / MainWindow
    ├── core/       Types / Sentence / Tokenizer(M11) / SubtitleModel / MediaEngine(抽象)
    │               MpvMediaEngine / QtMediaEngine / Waveform
    │               AudioRecorder / AudioAnalysis / PronunciationPlayer(M11)
    │               PlaylistModel / BookmarkModel / ResumeStore(M10) / ReadingStore(M12)
    ├── subtitle/   SubtitleParser / SubtitleAligner / TranslationProvider
    ├── asr/        AsrEngine(抽象) / WhisperCppAsr / FasterWhisperAsr / AsrTaskManager
    ├── study/      RepeatSession / ShadowingSession / DictationSession / RetellSession(M12)
    │               SentenceBuilder / FlashcardModel / VocabularyModel / DanmakuController
    ├── net/        HttpClient / YtDlpRunner / DictionaryProvider / DictionaryParser(M11)
    ├── ui/         PlayerControls / WaveformWidget / TranscriptPanel
    │               SubtitleOverlay / DictionaryPopup / DanmakuWidget / SoundSearchDialog(M11)
    │               RepeatControls / DictationWidget / SentenceBuilderWidget / RetellWidget(M12)
    │               SettingsDialog
    └── util/       Subprocess / TimeUtil / StringUtil / JapaneseInput(M11)
```

## 2. 构建说明（Windows + MSVC + Qt6，M9 已实际验证）

```bat
:: 前置：Qt 6.5+（含 Widgets/Multimedia/Network/Concurrent）、CMake 3.21+、MSVC 2022
call "C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Auxiliary\Build\vcvars64.bat"
cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release ^
      -DCMAKE_PREFIX_PATH=C:/Qt/6.8.3/msvc2022_64
cmake --build build -j 8
:: 产物：build/src/AdoLoop.exe
```

> M9（2026-09-21）已用 MSVC 19.44 + Qt 6.8.3 实机构建：configure / build 退出码均 0，
> clean build 44/44、零 warning，产物 `build/src/AdoLoop.exe`（564 KB）。
> 后续里程碑持续保持零 error / 零 warning：M10 47/47（644,608 字节）、M11 52/52（771,584 字节）、
> **M12 55/55（904,192 字节）**。构建过程中修复的问题清单见 `docs/11-进度日志.md`。
> 注意：PATH 中 MinGW 的 `gcc` 若排在 `cl.exe` 之前，CMake 会误选 GNU 工具链，
> 需把 MinGW 目录置于 vcvars 之后（必要时显式 `-DCMAKE_MAKE_PROGRAM=<ninja 路径>`）。

## 3. 外部工具与降级矩阵

| 组件 | 用途 | 缺失时行为 |
| --- | --- | --- |
| mpv.exe | 首选播放后端（JSON IPC，变速不变调） | 自动切 Qt Multimedia 后端（变速可能变调） |
| ffmpeg.exe | 波形解码、音频提取 | 波形区显示提示，不崩溃 |
| whisper-cli(.exe) | ASR 引擎 A（whisper.cpp） | 引擎 A 不可用，提示改用 faster-whisper |
| python3 + faster-whisper | ASR 引擎 B | 引擎 B 不可用，提示安装 |
| yt-dlp | 在线播放解析/字幕下载 | 打开 URL 时提示 |
| MeCab（M11，可选） | 日语分词增强（词/读音/原形） | 不触发该档，改用 python 桥或内置启发式 |
| python3 + janome/fugashi（M11，可选） | 日语分词增强（同上，经桥脚本） | 自动降级到内置启发式（默认档） |

所有外部工具路径可在「设置 → 通用/引擎」中显式配置，默认在 PATH 中探测
（`Subprocess::findExecutable`）。
**分词器两档外部引擎只在用户填了路径时才尝试**，因此默认情况下不会启动任何子进程。

## 4. 已实现模块速览（M2 起持续增补，表中为各里程碑新增/变更项）

| 文件 | 状态 | 说明 |
| --- | --- | --- |
| src/core/Types.h | ✅ | 公共类型：Lang/DictEntry/Sentence 前置声明/AsrSegment/TimeRange/SegmentParams/ShadowingScore |
| src/core/Sentence.{h,cpp} | ✅ | 语言感知分词（M11）、词级时间戳插值、意群切分、挖空、取词区间 |
| src/core/Tokenizer.{h,cpp} | ✅ | M11 语言感知分词（内置日语启发式 + MeCab/Python 桥三级降级） |
| src/core/SubtitleModel.{h,cpp} | ✅ | 句容器、二分定位、双语合并、ASR 导入、SRT 导出、断点合并/切分、语言注入 |
| src/core/PronunciationPlayer.{h,cpp} | ✅ | M11 词典发音（独立 QMediaPlayer，不干扰 mpv 主播放） |
| src/core/ReadingStore.{h,cpp} | ✅ | M12 读音缓存：词 → 假名/声调/词性/语言，查词成功写入，离线复用（readings.json） |
| src/study/RetellSession.{h,cpp} | ✅ | M12 段落复述状态机（句/意群/段落/全文，可选录音，无麦克风降级，retell.json） |
| src/ui/RetellWidget.{h,cpp} | ✅ | M12 复述面板（右 Tab 第 4 页；只做展示与收集操作） |
| src/net/DictionaryParser.{h,cpp} | ✅ | M11 有道 jsonapi 解析（日语 jc / 英语 ec，纯函数可单测；M12 补词性 pos） |
| src/util/JapaneseInput.{h,cpp} | ✅ | M11 罗马字→假名/片假名、假名互转、输入判定 |
| src/ui/SoundSearchDialog.{h,cpp} | ✅ | M11 听音查字对话框（输入/录音→ASR→查词） |
| src/app/Settings.{h,cpp} | ✅ | QSettings 单例：目录/播放/工具路径/ASR/断句/学习/词典悬停/快捷键（含默认键位表） |
| src/util/Subprocess.{h,cpp} | ✅ | 同步子进程 + 超时 + PATH 探测 + stdin 输入（M11，供分词桥用） |
| src/util/TimeUtil.{h,cpp} | ✅ | SRT/VTT/LRC 时间互转 |
| src/util/StringUtil.{h,cpp} | ✅ | 分词/标点（含全角与日文约物）/编辑距离/听写归一化 |

## 5. 关键编译注意点（自检记录）

- `Sentence::chunks()` 使用 `QRandomGenerator`（弃用 `qsrand/qrand` 已规避）。
- `Settings.h` 对 `QSettings` 采用前置声明。
- CMake 开启 `AUTOMOC`，所有 QObject 子类信号/槽按惯例放头文件。
- 本目录文档为过程记录，随里程碑推进更新。
